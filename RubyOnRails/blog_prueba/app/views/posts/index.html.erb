<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Posts</title>
</head>
<body>
	<%= link_to "New Post", new_post_path %>
	<h1>Lista de posts</h1>
	<ul>
		<% @posts.each do |post| %>
		<li>
			<%= post.title %>
			<br>
			<%= post.content %>
			<br>
			<%= link_to "Edit Post", edit_post_path(post) %>
			<br>
			<%= link_to "Delete Post", post_path(post), method: :delete,  data:{ confirm: "Estas seguro?"} %>
			<br>
			<%= link_to "Details", post_path(post) %>
		</li>
		<br>
		<% end %>
	</ul>
</body>
</html>
